# Neon PostgreSQL connection string
DATABASE_URL=
# Better Auth secret — 32+ chars (generate via: openssl rand -base64 32)
BETTER_AUTH_SECRET=
# App base URL (e.g. http://localhost:3000)
BETTER_AUTH_URL=
# OpenAI API key for AI card generation
OPENAI_API_KEY=
# Optional log verbosity: debug | info | warn | error (default: info in prod, debug in dev)
LOG_LEVEL=
